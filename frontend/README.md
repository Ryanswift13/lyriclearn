# LyricLearn 前端

基于 React 19 + TypeScript + Vite 8 构建的单页应用。

## 开发命令

```powershell
npm install          # 安装依赖
node node_modules\.bin\vite.CMD   # 启动开发服务器（port 5173）
npx tsc --noEmit    # 类型检查
npx eslint .        # 代码检查
```

> 启动前请确保后端服务已运行在 port 3000，详见根目录 README。
